{
  "name": "monitor",
  "productName": "BeeWatch",
  "version": "1.0.0",
  "description": "The GUI for Beewatch",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "node rebuild-packager . Beewatch --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/bee.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Beewatch\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "deep-equal": "^1.0.1",
    "materialize-css": "^1.0.0-rc.2",
    "serialport": "^7.0.2"
  },
  "devDependencies": {
    "electron": "^3.0.7",
    "electron-packager": "^12.2.0",
    "electron-rebuild": "^1.8.2"
  }
}
