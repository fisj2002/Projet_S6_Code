{
  "name": "monitor",
  "version": "1.0.0",
  "description": "The GUI for Beewatch",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "electron-rebuild -f -w monitor",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serialport": "^7.0.2"
  },
  "devDependencies": {
    "electron": "^3.0.7",
    "electron-rebuild": "^1.8.2"
  }
}
